package edu.val.palabrick.util;

/**
 * EN ESTA CLASE, NOS VAMOS A OCUPAR DE PROPORCIONAR LAS PALABRAS
 */
public class PalabraService {

    public static String obtenerPalabraDeLongitud5() {
        String palabra = null;
        //TypedArray XML - ANDROID
        /*TypedArray array_categorias = getResources().obtainTypedArray(R.array.array_categorias);
        CharSequence [] array_especifico = array_categorias.getTextArray(pos);
        array_categorias.recycle();

        String palabra = palabraOculta(array_especifico);
*/ //apéndice...cómo accedo a los Recursos (RES) desde fuera de una Actividad - CONTEXTO
        return palabra;
    }

    public static String obtenerPalabraDeLongitud4() {
        String palabra = null;

        return palabra;
    }

    public static String obtenerPalabraDeLongitud3() {
        String palabra = null;

        return palabra;
    }
}
